services:
  - type: web
    name: gkl-command-center
    runtime: node
    buildCommand: echo "ready"
    startCommand: node dashboard/server.js
    envVars:
      - key: NODE_ENV
        value: production
